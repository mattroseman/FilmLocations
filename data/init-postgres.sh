#!/bin/bash
set -e

psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
    CREATE USER $FILM_LOCATIONS_USER WITH PASSWORD '$FILM_LOCATIONS_PASS';
    GRANT ALL PRIVILEGES ON DATABASE $POSTGRES_DB TO $FILM_LOCATIONS_USER;
EOSQL
